---
interface Props {
  label: string;
  tint?: string;
}

const { label, tint } = Astro.props;
---

<span
  class:list={[
    'border text-xs font-mono px-2.5 py-1 rounded-full',
    tint ?? 'bg-surface border-stroke-subtle text-secondary',
  ]}
>
  {label}
</span>
